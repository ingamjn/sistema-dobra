'use client';

import React from 'react';

// Здесь будет минимальный пример или твой реальный код.
// Если 'data' не используется, просто удаляем её объявление.

export default function ReceivePage() {
  // Удаляем неиспользуемую переменную 'data'
  // const { data } = ... // удаляем или комментируем
  return (
    <main className="p-4">
      <h1>Receive Page</h1>
    </main>
  );
}
